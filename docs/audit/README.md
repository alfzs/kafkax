# Аудит kafkax v2

**Статус:** 🟡 частично — 68 из 82
**Источник:** комплексный аудит пакета, 2026-07-29
**Ревизия аудита:** `392b819` — изменений в код аудит не вносил. Правки волн 1–4 легли
поверх, в текущем дереве номера строк из старых формулировок могут не сойтись.

Восемь направлений, восемь субагентов. Все спорные утверждения перепроверены по исходникам
пакета и по franz-go v1.21.5 / kotel v1.7.0 в `go/pkg/mod`; в файлах категорий отмечено,
что именно проверено вручную.

## Категории

| # | Направление | Файл | Статус |
|---|---|---|---|
| 1 | Конкурентность и жизненный цикл | [01-concurrency.md](01-concurrency.md) | 🟢 закрыто — 6 из 6 |
| 2 | Kafka-семантика, оффсеты, ребаланс | [02-kafka-semantics.md](02-kafka-semantics.md) | 🟢 закрыто — 7 из 7 |
| 3 | Наблюдаемость | [03-observability.md](03-observability.md) | 🟢 закрыто — 15 из 15 |
| 4 | Ошибки и контракт API | [04-api-errors.md](04-api-errors.md) | 🟡 частично — 11 из 12 |
| 5 | Безопасность | [05-security.md](05-security.md) | 🟢 закрыто — 4 из 4 |
| 6 | Тесты | [06-tests.md](06-tests.md) | 🟡 частично — 7 из 18 |
| 7 | Производительность и аллокации | [07-performance.md](07-performance.md) | 🟡 частично — 3 из 5 |
| 8 | Идиоматичность, стиль, документация | [08-style-docs.md](08-style-docs.md) | 🟡 частично — 14 из 15 |

Задач меньше, чем исходных пунктов аудита: в 03 и 04 раздел «Мелочи» свёрнут в одну задачу
P3 — пункты внутри сохранены дословно. В 06 и 08, где у каждой мелочи был свой разбор,
они остались отдельными задачами.

Дубликаты между направлениями сведены: находка живёт в одном файле, из остальных на неё ссылка.

## Контекст

Пакет собирается, `go vet`, `make lint`, `make audit` и `go test -race -count=5` — чисто,
покрытие на момент аудита 89.7%. Все найденные дефекты лежали **вне happy path**: в путях
остановки, ребаланса, паники в обвязке и отказа брокера. Ровно поэтому их не видели ни
тесты, ни линтеры.

Три критические находки конкурентности имели **одну корневую причину**, и это самое важное,
что дал аудит целиком.

### Корневая причина №1 — у остановки нет единого владельца

🟢 **Закрыто волной 1.** Рефакторинг сделан одним заходом, как и предполагалось.

`Stop()` был единственным путём, который действительно всё закрывал. Но остановка могла прийти
ещё двумя способами, и оба были неполны:

* **отмена `ctx`, переданного в `Start`** → `watchContext` (`consumer.go:382-389`) делал только
  `lifeCancel()`. Цикл опроса выходил, а `kgo.Client` оставался жив, продолжал слать heartbeat
  и **навсегда** держал членство в группе — партиции не переназначались никогда, гейт
  `BlockRebalanceOnPoll` оставался зажатым. Документация `Start` при этом прямо называла
  отмену `ctx` эквивалентом `Stop`;
* **`Stop`, не уложившийся в `GracefulTimeout`** (`consumer.go:971-978`) → `stopAllWorkers`
  намеренно пропускался, но `CloseAllowingRebalance()` 18 строк спустя вызывался **всё равно**,
  при живом цикле опроса — и делал ровно то, что комментарий этой же ветки запрещал.

Плюс окно между проверкой `stopping` (`consumer.go:337`) и публикацией клиента (`:368-371`):
`Stop`, попавший в него, видел `client == nil`, уходил по ранней ветке и навсегда взводил
`stopping` — созданный клиент оставался без закрывающего.

Отсюда же вытекали: застревающий `workers.active`, утечка горутин-воркеров, неотданные
партиции, гонка на `c.workers` и `panic: send on closed channel` в цикле опроса, у которого
**единственного в пакете не было `recover`**.

Лечилось одним рефакторингом остановки, а не пятью точечными правками. Детали — в
[01-concurrency.md](01-concurrency.md).

### Корневая причина №2 — `recover` без «отравления» ломает инвариант оффсетов

🟢 **Закрыто волной 1.**

`processRecord` ловил панику в обвязке (`consumer.go:556-565`), рапортовал и **давал функции
вернуться штатно** — без `w.poisoned` и без отметки. Цикл переходил к следующей записи, и при
её успехе `MarkCommitRecords` двигал коммит **за необработанную**. Собственный комментарий на
`consumer.go:619-621` формулировал нарушенный инвариант дословно. Это была тихая потеря
сообщения. Детали — в [02-kafka-semantics.md](02-kafka-semantics.md).

### Корневая причина №3 — телеметрия не переживает деградацию

🟢 **Закрыто волнами 1 и 2.** Гейдж, бакеты и атрибут `topic` исправлены волной 1;
продолжающийся сигнал приостановленной партиции (`partitions.paused`) и дедуп потока ошибок
фетча — волной 2.

Гейдж `workers.active` врал (см. №1); обе гистограммы объявляли `WithUnit("s")` и оставляли
дефолтные бакеты SDK, которые миллисекундные, — вся латентность падала в первый бакет; атрибут
`topic` у продюсера брался из недоверенного входа **до** валидации; у приостановленной партиции
не было ни одного продолжающегося сигнала. Детали — в [03-observability.md](03-observability.md).

## Порядок работ

**Волна 1 — блокеры перехода потребителей** (без них внедрять было нельзя): 🟢 закрыта

1. 🟢 Единый путь остановки: `watchContext` инициирует полное завершение; `Stop` не закрывает
   клиента при живом цикле опроса; `c.workers` либо под мьютексом, либо доказуемо
   однопоточная; `recover` в `runPollLoop`. — RF-CONC-01, RF-CONC-02, RF-CONC-03
2. 🟢 `recover` в `processRecord` травит партицию. — RF-KAFKA-01
3. 🟢 Бакеты гистограмм. — RF-OBS-02 *(одна строка, эффект — рабочая латентность)*
4. 🟢 `topic` у продюсера вычислять после валидации. — RF-OBS-03

**Волна 2 — операционные дыры:** 🟢 закрыта

5. 🟢 Снятие паузы с отравленной партиции при кооперативном ребалансе. — RF-KAFKA-03
6. 🟢 Метрика `partitions.paused`, дедуп потока `fetch.errors`, `status="cancelled"`. —
   RF-OBS-05, RF-OBS-06, RF-OBS-10, RF-OBS-11
7. 🟢 Тесты на непокрытые классы: потеря партиций, отказ финального коммита, паника воркера,
   редакция секретов, `goleak`, `Int64UpDownCounter` в `recordingMeter`. — RF-TEST-01…RF-TEST-06,
   RF-TEST-08, RF-TEST-09
8. 🟢 Сентинелы `ErrFlushIncomplete` / `ErrCommitFailed` / `ErrInvalidConfig` /
   `ErrDuplicateHandler`, `DefaultConfig()`. — RF-API-01…RF-API-04

**Волна 3 — качество и цена:** 🟢 закрыта

9. 🟢 Кэш опций метрик и ленивый логгер. — RF-PERF-01, RF-PERF-02
10. 🟢 SASL PLAIN без TLS, редакция пароля, валидация размеров fetch. — RF-SEC-01, RF-SEC-02,
    RF-SEC-04
11. 🟢 *(кроме владения буфером)* Документация и стиль: `status="ok"` → `success`, мёртвая ссылка
    `SubscribeAll`, `make fmt` переписывает исходники. Контракт владения буфером
    `IncomingMessage` остался открытым — RF-API-05.

**Волна 4 — контракт API и остаток мелочей:** 🟢 закрыта

12. 🟢 Контракты, которые нельзя описать документацией задним числом: владение буфером
    `IncomingMessage`, типизированные части ключа вместо `...any`, причина рядом с сентинелом в
    `produceError`, контракт вызова `OnMessageSkipped`, тип `PanicSite`, три состояния
    жизненного цикла консьюмера, `DeliveryError` вместо `*kerr.Error` в разборе отказа. —
    RF-API-05…RF-API-11
13. 🟢 Единственная точка правды о длине ключа — `encoding.Key`; кодирование ключа за одну
    аллокацию. — RF-DOCS-07, RF-PERF-05
14. 🟢 Остаток мелочей: предпосылки целей `Makefile`, уборка файлов покрытия, обязательные поля
    в README, `LICENSE`, явное отключение `contextcheck` с измеренной причиной. —
    RF-DOCS-11…RF-DOCS-15

**Волна 5 — читаемость отказа:** 🟢 закрыта

15. 🟢 Сигналы, по которым отказ отличают от исправной работы: baggage доезжает до обработчика,
    рапорт о панике пишется на сообщение, а не на попытку, один отказ даёт одну запись в
    журнале вместо трёх, паника внутри `OnPanic` перестала быть молчаливой. — RF-OBS-04,
    RF-OBS-07, RF-OBS-12, половина RF-OBS-15
16. 🟢 Два пути, на которых оффсет вёл себя не так, как обещано: воркер, переживший жёсткую
    отмену, больше не отмечает оффсет отданной партиции; погибший воркер больше не превращает
    партицию в чёрную дыру. — остаток RF-KAFKA-04, RF-KAFKA-07

**Волна 6 — контракт телеметрии:** 🟢 закрыта

17. 🟢 Три события, означающие потерю или дублирование данных, перестали существовать только в
    логе: `commit.errors{phase}`, `partitions.lost`, `drain.timeouts{phase}`. Заодно правило
    «есть кому вернуть — не логируем» применено к `Stop` и `Close`. — RF-OBS-08
18. 🟢 Потеря данных на брокере отделена от обычного сбоя фетча атрибутом `reason`. —
    RF-KAFKA-06
19. 🟢 `Config.KafkaLogLevel` отвязывает многословность franz-go от уровня логгера приложения. —
    RF-OBS-14
20. 🟢 Метрики перестали обещать то, чего нет: комментарии про kotel, scope с версией, единицы
    у счётчиков, письменно зафиксированное отклонение от messaging semantic conventions. —
    RF-OBS-13, остаток RF-OBS-15
21. 🟢 Две оговорки, которых не хватало в документации: `MessageTimeout` не жёсткий бюджет при
    идемпотентности, `MessageQueueSize` — это ещё и потолок памяти. — RF-KAFKA-05, RF-SEC-03

## Что закрыто волной 6

| Находка | Что изменилось |
|---|---|
| RF-OBS-08 | `kafkax.consumer.commit.errors{phase}`, `kafkax.consumer.partitions.lost`, `kafkax.consumer.drain.timeouts{phase}`. Правило дублирования: есть кому вернуть — не логируем. Лог убран из `Stop` и `KafkaProducer.flush`, оставлен в колбэке ребаланса (возвращать некому) и у `ErrPollLoopStuck` (возврат сообщает факт, но не последствие — открытый клиент). `partitions.lost` считает партиции, `drain.timeouts` — события |
| RF-KAFKA-06 | `*kgo.ErrDataLoss` и `OffsetOutOfRange` разобраны отдельными ветками, `reason` уходит и в лог, и в `fetch.errors`. Хук приложения не заводился: на это событие приложению нечего ответить |
| RF-OBS-14 | `Config.KafkaLogLevel` с экспортированными константами. Реализация — обёртка над `slog.Handler`: kslog выводит `kgo.LogLevel` из `Enabled`, поэтому franz-go при поднятом пороге не собирает сообщение вовсе. Только ужесточение — уровень логгера приложения остаётся внешним фильтром |
| RF-OBS-13 | Комментарии больше не обещают, что kotel измеряет латентность запросов: в v1.7.0 у него только счётчики. Godoc `instrumentationName` называет scope метрик и отдельный scope спанов kotel. Гистограмма на `CommitMarkedOffsets` не заводилась — две точки вызова за жизнь партиции распределения не дают |
| RF-OBS-15 | Остаток: `WithInstrumentationVersion` из build info (константу забыли бы обновить), `WithUnit` всем счётчикам в аннотационной форме — имена рядов в Prometheus не меняются. `WithSchemaURL` намеренно НЕ добавлен: метрики от messaging semantic conventions отклоняются, и объявление схемы заставило бы схемо-осведомлённый backend переименовывать атрибуты по чужим правилам |
| RF-KAFKA-05 | Godoc `MessageTimeout`, README и `doc.go`: при `EnableIdempotence=true` это верхняя граница ожидания вызывающим, а не гарантия отмены записи |
| RF-SEC-03 | Godoc `MessageQueueSize` и README: ёмкость в батчах, формула потолка памяти и число для умолчаний (~3 ГиБ). Байтовый семафор отвергнут — уже существующая ручка закрывает задачу, если её посчитать |

## Что закрыто волной 5

| Находка | Что изменилось |
|---|---|
| RF-OBS-04 | `processRecord` переносит в контекст обработчика не только спан, но и baggage из `rec.Context`. Основой остаётся контекст воркера — отмена обязана доходить, — поэтому переносится именно baggage, а не контекст целиком. Под `Len() > 0`: propagator по умолчанию baggage не ставит, и без проверки пустое значение клалось бы в контекст на каждое сообщение |
| RF-OBS-07 | `callHandler` принимает номер попытки и рапортует панику только на первой: стек, `panics{site=handler}`, `OnPanic`. Вариант «сделать `ErrHandlerPanic` неретраибельным» отвергнут — он лишает смысла `HandlerMaxRetries=-1`, отправляя паникующее сообщение в пропуск немедленно |
| RF-OBS-12 | Уровень записи выбирает только `resolveFailure`. `runHandler` на исчерпании молчит, сам `resolveFailure` тоже: всё, что известно об отказе — попытки, ветка разрешения, ошибка хука, — уходит атрибутами в единственную запись `poison`. Штатный пропуск через работающий хук перестал поднимать Error |
| RF-OBS-15 | Половина: двойной `RecordError` ушёл (`callHandler` спан не трогает вовсе), `callHook` пишет стек и инкрементит `panics{site=on_panic}`. Scope метра и `WithUnit` не трогались — `WithSchemaURL` заводит новые серии, это решение вместе с RF-OBS-13 |
| RF-KAFKA-04 | Остаток закрыт проверкой `ctx.Err()` перед `MarkCommitRecords`. Отдельный флаг «партиция отдана» не заводился: жёсткая отмена контекста воркера и есть этот флаг, и она уже расставлена там, где нужно. На мягкой остановке проверка не срабатывает — там контекст воркера живой |
| RF-KAFKA-07 | Ветка `<-worker.done` в `dispatch` выводит партицию из выборки и считает батч как `dropped`. Пересоздание воркера отвергнуто: умер он от паники в собственном теле, и новый упал бы так же — цикл смертей вместо одного отказа |
| RF-DOCS-06 | Расхождение шапки `panics.go` с кодом снято правкой кода, а не комментария: шапка обещала верное, молчал `callHook`. Godoc `Config.OnPanic` дополнен обоими фактами — метрика пишется, хук повторно не зовётся |

## Что закрыто волной 1

Сделано вместе с волной 1 сверх её списка: находки, оказавшиеся тем же кодом или той же строкой
документации, что и запланированные, — чинить их отдельным коммитом значило бы трогать те же
строки дважды.

| Находка | Что изменилось |
|---|---|
| RF-CONC-01 | `watchContext` зовёт `Stop`, а не голый `lifeCancel`: отмена `ctx` доводит завершение до финального коммита и закрытия клиента |
| RF-CONC-02 | Проверка `stopping` и публикация `c.client` — одна критическая секция под `c.mu` |
| RF-CONC-03 | При невышедшем цикле опроса клиент намеренно НЕ закрывается; `Stop` возвращает новый `ErrPollLoopStuck` |
| RF-CONC-04 | `awaitWorkers` после жёсткой отмены дожидается воркеров — одним общим бюджетом на всех оставшихся, а не по одному |
| RF-CONC-05 | Godoc `Stop` называет реальный худший бюджет и советует учесть его в `terminationGracePeriodSeconds` |
| RF-CONC-06 | Контекст таймаута переименован в `sendCtx`; метрики продюсера пишутся пользовательским контекстом |
| RF-KAFKA-01 | `recover` в `processRecord` травит партицию; регрессионный тест `TestPanicInProcessingWrapperPoisonsPartition` проверен на отключённом фиксе |
| RF-KAFKA-02 | Следствие RF-CONC-01 |
| RF-KAFKA-04 | Основной путь закрыт вместе с RF-CONC-04; остаточный случай (воркер не вышел и после жёсткой отмены) теперь логируется Error |
| RF-OBS-01 | Воркер читает отмену наравне с очередью, отмена `ctx` доводит до `stopAllWorkers` — гейдж больше не застревает |
| RF-OBS-02 | Явные границы бакетов в секундах: `producerDurationBuckets`, `consumerDurationBuckets` в `otel.go`; проверяются в `otel_test.go` |
| RF-OBS-03 | Валидация до регистрации метрик исхода; отбраковка — свой счётчик `kafkax.producer.messages.rejected` с замкнутым `reason` и без `topic` |
| RF-OBS-09 | README приведён к `success` |
| RF-DOCS-01, RF-DOCS-03, RF-DOCS-04, RF-DOCS-05, RF-DOCS-08, RF-DOCS-09 | Документация и `Makefile` — см. файл |
| RF-DOCS-02 | Частично: `doc.go` разводит два разных `skipped`. Отдельное значение `"cancelled"` было отложено волне 2 (RF-OBS-10) — там и сделано |

## Что закрыто волной 2

| Находка | Что изменилось |
|---|---|
| RF-KAFKA-03 | Пауза принадлежит воркеру, а не назначению: собственный набор `c.paused`, снятие в `resumePartition` при создании воркера и при его остановке. Резюмирование по списку `assigned` отвергнуто — оно вернуло бы в выборку партицию с живым отравленным воркером |
| RF-OBS-05 | `kafkax.consumer.partitions.paused` (`Int64UpDownCounter`, инкремент идемпотентен — гейдж считает партиции, а не сообщения) и `status="dropped"` для записей, выброшенных отравленным воркером; снятие паузы логируется |
| RF-OBS-06 | `c.lastFetchErr`: лог и счётчик ошибок фетча срабатывают на смену состояния, а не на каждый опрос; отметка сбрасывается, когда партиция снова отдала записи |
| RF-OBS-10 | `status="cancelled"` отделён от `skipped`: коммит при отмене не двигается, и всплеск на деплое больше не читается как потеря данных |
| RF-OBS-11 | `kafkax.consumer.group.errors` — отдельный счётчик для `*kgo.ErrGroupSession`, который franz-go подкидывает синтетическим фетчем с пустым топиком |
| RF-TEST-05 | `recordingMeter` умеет `Int64UpDownCounter`; ассерт «после `Stop` == 0» есть для `workers.active` и `partitions.paused` |
| RF-TEST-08 | `reportFetchError` покрыт напрямую: дедуп, смена текста, раздельный учёт партиций, сброс после выздоровления, обе молчаливые ветки, разделение группового и партиционного отказа |
| RF-DOCS-02 | `doc.go`, README и таблица статусов в README согласованы с кодом |
| RF-API-01 | `ErrFlushIncomplete` и `ErrCommitFailed`: два исхода завершения, означающие потерю или дублирование данных, отличимы без `strings.Contains`. Причина коммита сохранена рядом с сентинелом |
| RF-API-02 | `ErrDuplicateHandler`; тест на повторную регистрацию больше не фиксирует текст сообщения как контракт |
| RF-API-03 | Тип-агрегат `configError` вместо `errors.Join` с подмешанным сентинелом: `errors.Is(err, ErrInvalidConfig)` работает, а `Unwrap() []error` отдаёт только претензии к полям. Конструкторы отдают агрегат как есть — обёртка сломала бы документированный разбор списка |
| RF-API-04 | `DefaultConfig()` — 29 значений из тегов `env-default`; `Brokers`/`ClientID`/`Consumer.Group` пусты намеренно. Дубль защищён двусторонней сверкой с тегами через рефлексию |
| RF-TEST-02 (часть) | `TestStopReportsFailedFinalCommit`: кластер гасится после обработки, но до `Stop`. Отказ финального коммита при `Stop` покрыт; отказ при revoke — нет |
| RF-TEST-01 | `TestPartitionsLostDoesNotCommit`: подмена ответа на `Heartbeat` кодом `UNKNOWN_MEMBER_ID` — единственный код, ведущий в `onLost` мимо штатного отзыва. Подменяется ответ, а не состояние группы в kfake: участник для брокера жив, его коммит был бы принят, поэтому «оффсет не сдвинулся» доказывает решение библиотеки, а не отказ брокера |
| RF-TEST-03 | `config_redaction_test.go`: пароль-канарейка ищется подстрокой в выводе обоих хендлеров slog и глаголов `%v`/`%s`/`%+v`. Плюс три теста-сенсора, фиксировавших утечку через `%#v` и `json.Marshal` как факт — перевёрнуты волной 3 вместе с RF-SEC-02 |
| RF-TEST-04 | `consumer_worker_panic_test.go`: паника ставится на отладочную запись о старте воркера — единственную точку `runPartitionWorker` вне `processRecord`, где исполняется чужой код. Ветки `<-worker.done` и `<-ctx.Done()` в `dispatch` покрыты через очередь на один батч и две записи из **разных** опросов, синхронизованных хуком `HookFetchRecordUnbuffered` |
| RF-TEST-06 | `goleak.VerifyTestMain` **с пустым списком исключений**: ни пакет, ни franz-go, ни kotel, ни kfake не оставляют горутин. Оснастка проверена намеренной утечкой, а не принята по зелёному прогону |
| RF-TEST-07 | Закрыт как «не нужен»: предложенная `failRequests(..., times int)` не подошла ни одному из двух реальных вызовов — RF-TEST-01 нужен арм/дизарм, RF-TEST-09 снятия не требует вовсе. Обобщать нечего |
| RF-TEST-09 | `consumer_graceful_degradation_test.go`: `workers.active == 1` сразу после `Stop` доказывает, что воркер брошен, а не дождался; `ErrPollLoopStuck` снят подвешиванием цикла опроса через `slog.Handler` — путь существует потому, что kafkax отдаёт логгер в franz-go через kslog |

Покрытие пакета: **89.4% → 94.6%**, функций с 0.0% не осталось ни одной (было шесть).

Отрицательный результат: гонку из RF-CONC-03 воспроизвести под `-race` не удалось
(пробы на 460–600 мс, `-count=4` и `-count=8`). После правки волны 1 это ожидаемо, но
регрессионного теста на саму гонку нет — закреплено только её наблюдаемое следствие.

## Что закрыто волной 3

| Находка | Что изменилось |
|---|---|
| RF-PERF-01 | `optsCache`: готовые срезы `[]metric.AddOption`/`[]metric.RecordOption` по ключу `(topic, status)` на `atomic.Pointer` поверх copy-on-write map — чтение lock-free и без аллокаций. `sync.Map` отвергнут: упаковка ключа из двух строк в `any` аллоцирует на каждом `Load`. У консьюмера прогрев из `AddHandler` без роста дальше, у продюсера — потолок 256, потому что топик приходит из `PublishRequest`, то есть из недоверенного входа; безлимитная map на таком ключе была бы ровно той утечкой, что описана в RF-OBS-03 |
| RF-PERF-02 | `recordLogger` откладывает `logger.With(offset, trace_id)` до первого обращения; `gcflags=-m` подтверждает, что структура не убегает в кучу |
| RF-SEC-01 | `PLAIN` без TLS — ошибка валидации, а не предупреждение, с именованным опт-аутом `SASL.AllowPlaintext`. Предложенный аудитом чистый WARN отвергнут: отключённая проверка сертификата создаёт уязвимость, а `PLAIN` без TLS выполняет раскрытие — пароль уходит открытым текстом при каждой аутентификации, и правкой конфигурации это не чинится, секрет придётся ротировать. `SCRAM` без TLS остался предупреждением: пароль по проводу не идёт |
| RF-SEC-02 | `SASL.GoString`, `SASL.MarshalJSON`, `Config.LogValue`. Пароль редактируется на всех путях вывода; `slog.Any("config", cfg)` на `JSONHandler` больше не отдаёт `!ERROR:json: unsupported type`. Четыре теста-сенсора перевёрнуты, один — переформулирован: `json.Marshal(Config)` по-прежнему падает на func-полях, но это стало констатацией, а редакция проверяется на структуре-двойнике |
| RF-SEC-04 | `fetchSizeErrors`: `min_bytes`/`max_bytes`/`max_partition_bytes` > 0 и `max_partition_bytes ≤ max_bytes`; `producer.max_buffered_bytes ≥ 0`. franz-go эти поля не проверяет: ноль проходит конструктор клиента, а отказ выглядит как «консьюмер подключился и ничего не читает» |

Измерено (benchstat, `-benchmem -count=6`; базой была копия пакета с откатанными телами
горячих путей, то есть код бенчмарков по обе стороны идентичен):

| Бенчмарк | sec/op | allocs/op | B/op |
|---|---|---|---|
| `ProducerRecordSend/resolving` | 694.7n → 101.7n (−85.4%) | 8 → 0 | 464 → 0 |
| `ConsumerRecordOutcome/resolving` | 1126n → 70.7n (−93.7%) | 8 → 0 | 592 → 0 |
| `ConsumerCountMessage/resolving` | 408.2n → 53.6n (−86.9%) | 4 → 0 | 296 → 0 |
| `RecordLoggerUnused` | 1136.5n → 3.9n (−99.7%) | 15 → 0 | 672 → 0 |
| `RecordLoggerUsed` | 1.900µ → 2.013µ (+5.95%) | 15 → 15 | 672 → 672 |

geomean −92.01%, все p=0.002. Регресс `RecordLoggerUsed` намеренный: путь отказа делает ту же
работу плюс инициализацию структуры и проверку на nil, аллокации идентичны — отказы редки,
счастливый путь проходит каждое сообщение. Прогноз аудита (−33% аллокаций, оверхед ×1.32 → ×1.08)
оказался сильно консервативнее факта; e2e-замер после правок не повторялся, подробности — в
[07-performance.md](07-performance.md).

Покрытие пакета: **94.6% → 95.3%**, функций с 0.0% по-прежнему ни одной. Бенчмарки появились в
репозитории впервые — раньше не было ни одного.

`RF-SEC-01` — ломающее изменение: конфигурация с `sasl.mechanism=PLAIN` без TLS больше не
проходит `Validate`.

## Что осталось открытым

14 задач из 82: 12 открыты, две закрыты наполовину (RF-API-12, RF-TEST-02), одна отклонена
(RF-TEST-07). Четыре из восьми направлений закрыты целиком. Ни одной P0 не осталось; из P1
открыт только RF-TEST-10.

* **[06-tests.md](06-tests.md)** — 9 открытых. Пять из них (RF-TEST-11…RF-TEST-14, RF-TEST-16)
  об одном и том же: доказательство «ничего не произошло» держится на `time.Sleep`, а ассерт —
  на настенных часах.
* **[07-performance.md](07-performance.md)** — 2 открытых: `context.WithTimeout` на каждый
  `SendMessage` (RF-PERF-03), `handlersMu.RLock` на неизменяемой карте (RF-PERF-04).
* **[08-style-docs.md](08-style-docs.md)** — 1 открытая: `consumer.go` в трёх слоях одним
  файлом (RF-DOCS-10).
* **[04-api-errors.md](04-api-errors.md)** — открытых нет, RF-API-12 закрыта наполовину.

## Проверка

```bash
go build ./...
go vet ./...
make lint
make test
make test-cover
go test -race -count=2 ./...
go test -run='^$' -bench=. -benchmem ./...
```

На момент закрытия волны 3: `make lint` — 0 замечаний, `go test -race -count=2` — зелёный,
покрытие 95.3% (kafkax) и 96.4% (encoding), функций с 0.0% нет.

## Что проверено и признано верным

Список большой и он важен не меньше находок: он показывает, где решения приняты осознанно и
трогать их не надо. Полностью — в разделах «Проверено и признано верным» каждого файла. Коротко:

* архитектура «горутина на партицию + `BlockRebalanceOnPoll`» корректна; отказ от тенантных
  воркеров ничего не сломал;
* политика отравленного сообщения (не коммитить, встать на паузу) реализована ровно так, как
  задокументирована, и это правильный дефолт;
* `onPartitionsLost` намеренно не коммитит — верно; отдельный бюджет на коммит при ребалансе —
  верно;
* трассировка продюсер↔консьюмер сквозная, висящих спанов нет;
* TLS: `MinVersion` 1.2, пиннинг через пустой пул, `InsecureSkipVerify` с WARN на обоих путях;
  утечки пароля в логи franz-go нет;
* все шесть Go-блоков README компилируются; все 41 значение по умолчанию сверены с тегами —
  расхождений ноль;
* `.golangci.yml` образцовый, все 26 `//nolint` с содержательным обоснованием;
* цепочка middleware строится один раз, `MatchKeyMiddleware` на сообщение делает 0 аллокаций;
* 74% замедления консьюмера против голого franz-go — это цена инструментации kotel,
  и только 26% — собственный код пакета.
